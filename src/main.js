import './styles.css';
import { initApp } from './app.js';

// Initialize the application when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
  console.log('Bernard Admin Dashboard - Starting...');
  initApp();
});
